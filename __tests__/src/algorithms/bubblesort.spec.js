const bubbleSort = require("../../../src/algorithms/bubblesort");

describe("bubbleSort", () => {
  it("should sort an array of numbers", () => {
    const input = [4, 11, 3232, 3, 19, 0, 9];
  });
});
